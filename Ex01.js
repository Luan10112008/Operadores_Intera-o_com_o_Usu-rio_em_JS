// Valor inicial
let cosmoBase = 100;

// Multiplica o valor atual por 5 (cosmoBase = cosmoBase * 5)
cosmoBase *= 5; 

// Subtrai 50 do valor resultante (cosmoBase = cosmoBase - 50)
cosmoBase -= 50;

console.log(cosmoBase);
